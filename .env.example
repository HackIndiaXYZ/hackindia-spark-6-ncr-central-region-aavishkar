# Copy to .env.local for local development. Do not commit .env.local

# Frontend
NEXT_PUBLIC_APP_URL=http://localhost:3000

# MongoDB (optional; used if you enable real DB in code)
MONGODB_URI=
MONGODB_DB=jan_setu

# Auth / Sessions
SESSION_SECRET=

# Email (optional)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
MAIL_FROM="JanSetu <no-reply@example.com>"

# AI Service (optional)
AI_SERVICE_URL=http://localhost:8000
